# Fly Brain Robot — Mac M1 (Python 3.10+)
numpy>=1.23
pillow>=10.0
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
pydantic>=2.0
# torch is optional; NumPy COO fallback is used if it is missing
# torch
# one-time connectome / skeleton fetch only:
# neuprint-python
# navis
