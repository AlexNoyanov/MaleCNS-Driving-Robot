# Copy to scripts/deploy-pi.env (gitignored) or export these in your shell.
PI_HOST=192.168.1.133
PI_USER=ynoyanov
PI_DIR=/home/ynoyanov/Work/AI-Brains/MaleCNS-Driving-Robot
# PI_PASSWORD=          # only if this Mac has no SSH key on the Pi
# MAC_HOST=             # defaults to this Mac's en0/en1 address
# MAC_PORT=8000
